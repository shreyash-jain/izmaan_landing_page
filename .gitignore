# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production / static export
/build
/dist

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env
# Wrangler's local secrets for `wrangler pages dev` — holds the admin password
# and, once you add them, the Google service-account credentials. Never commit.
.dev.vars
.dev.vars.*

# typescript
*.tsbuildinfo

# design handoff bundle (reference only — not part of the app)
/Luxury Lodge Website Design-handoff/
/Luxury Lodge Website Design-handoff.zip

# local tooling
/.claude/
/.wrangler/

# QA audit output (screenshots / reports from scripts/audit.mjs)
/.audit/
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
